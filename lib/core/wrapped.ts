import type { DailyCheckIn, JournalEntry, RewardEvent, Transaction } from "./types.js";import{buildFinanceSummary}from"./finance.js";
export interface WrappedStats{year:number;income:number;personalSpending:number;invested:number;saved:number;futureAllocation:number;topCategory:string|null;mostExpensiveMonth:string|null;lowestSpendMonth:string|null;movementDays:number;averageMood:number|null;checkInDays:number;journalDays:number;totalXp:number;coins:number;themeWords:string[]}
const STOP=new Set(["this","that","with","from","have","were","your","today","really","just","very","good","felt","into","about","after","before","there","their","would","could","should","what","when","where","which","been","being"]);
export function buildWrappedStats(year:number,transactions:Transaction[],checkIns:DailyCheckIn[],journal:JournalEntry[],rewards:RewardEvent[]):WrappedStats{const monthly=Array.from({length:12},(_,i)=>`${year}-${String(i+1).padStart(2,"0")}`).map(m=>buildFinanceSummary(m,transactions,[])),annualIncome=monthly.reduce((s,m)=>s+m.income,0),annualSpending=monthly.reduce((s,m)=>s+m.personalSpending,0),annualInvested=monthly.reduce((s,m)=>s+m.invested,0),annualSaved=monthly.reduce((s,m)=>s+m.saved,0),catTotals:Record<string,number>={};monthly.forEach(m=>Object.entries(m.categoryTotals).forEach(([k,v])=>catTotals[k]=(catTotals[k]||0)+v));const topCategory=Object.entries(catTotals).sort((a,b)=>b[1]-a[1])[0]?.[0]??null,spendingMonths=monthly.filter(m=>m.personalSpending>0),most=[...spendingMonths].sort((a,b)=>b.personalSpending-a.personalSpending)[0]?.period??null,low=[...spendingMonths].sort((a,b)=>a.personalSpending-b.personalSpending)[0]?.period??null,yearCheckins=checkIns.filter(c=>c.date.startsWith(String(year))),averageMood=yearCheckins.length?yearCheckins.reduce((s,c)=>s+Number(c.mood||0),0)/yearCheckins.length:null,yearJournal=journal.filter(j=>j.date.startsWith(String(year))),words=yearJournal.flatMap(j=>j.content.toLowerCase().replace(/[^a-z\s]/g," ").split(/\s+/)).filter(w=>w.length>=4&&!STOP.has(w)),counts=new Map<string,number>();words.forEach(w=>counts.set(w,(counts.get(w)||0)+1));const themeWords=[...counts.entries()].sort((a,b)=>b[1]-a[1]).slice(0,5).map(([w])=>w),yearRewards=rewards.filter(r=>r.date.startsWith(String(year)));return{year,income:annualIncome,personalSpending:annualSpending,invested:annualInvested,saved:annualSaved,futureAllocation:annualInvested+annualSaved,topCategory,mostExpensiveMonth:most,lowestSpendMonth:low,movementDays:yearCheckins.filter(c=>Number(c.movement_minutes||0)>0).length,averageMood,checkInDays:new Set(yearCheckins.map(c=>c.date)).size,journalDays:new Set(yearJournal.map(j=>j.date)).size,totalXp:yearRewards.reduce((s,r)=>s+Number(r.xp||0),0),coins:yearRewards.reduce((s,r)=>s+Number(r.coins||0),0),themeWords}}
export function wrappedTheme(s:WrappedStats){if(s.invested+s.saved>s.personalSpending*.35&&s.checkInDays>=180)return"The Year of Intentional Growth";if(s.checkInDays>=220)return"The Year of Showing Up";if(s.movementDays>=140)return"The Year of Finding Your Rhythm";if(s.futureAllocation>0)return"The Year of Building Future You";return"The Year of Small, Meaningful Steps"}
