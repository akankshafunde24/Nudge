export function requireEnv(name:string){const value=process.env[name];if(!value)throw new Error(`Missing required environment variable: ${name}`);return value}export function optionalEnv(name:string){const value=process.env[name]?.trim();return value||undefined}export const isProduction=()=>process.env.NODE_ENV==="production";export const devBypassEnabled=()=>process.env.NUDGE_DEV_BYPASS==="true"&&process.env.NODE_ENV!=="production";
