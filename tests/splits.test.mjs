import test from'node:test';import assert from'node:assert/strict';import{equalSplit,settleSplit,totalOpenReceivable}from'../.test-dist/lib/core/splits.js';test('equal split rounding preserves total',()=>{const r=equalSplit(100,['A','B']);assert.deepEqual(r.shares.map(s=>s.amount),[33.33,33.33]);assert.equal(r.personalAmount,33.34)});test('partial settlement never exceeds amount owed',()=>{const split={id:'s',transaction_id:'t',person:'A',total_bill:900,my_share:300,their_share:300,status:'pending',settled_amount:0},partial=settleSplit(split,100,'2026-09-06'),final=settleSplit(partial,999,'2026-09-07');assert.equal(partial.status,'partial');assert.equal(final.status,'settled');assert.equal(final.settled_amount,300)});test('open receivable includes older months',()=>{assert.equal(totalOpenReceivable([{id:'a',transaction_id:'old',person:'A',total_bill:600,my_share:300,their_share:300,status:'pending',settled_amount:0},{id:'b',transaction_id:'new',person:'B',total_bill:900,my_share:300,their_share:600,status:'partial',settled_amount:200}]),700)});
